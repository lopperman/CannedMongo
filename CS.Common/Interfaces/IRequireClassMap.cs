namespace CS.Common.Interfaces
{
    /// <summary>
    /// Used to tell if a class REQUIRES mapping in the Discriminator (MongoDiscriminitor).
    /// Don't delete this, even though it is empty! (paul brower)
    /// </summary>
    public interface IRequireClassMap
    {
    }
}
